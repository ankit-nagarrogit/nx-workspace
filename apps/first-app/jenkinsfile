pipeline {
  agent any

  tools {
    nodejs 'NodeJS_LTS'
  }

  environment {
    NX_SKIP_NX_CACHE = 'true'
  }

  stages {
    stage('Checkout') {
      steps {
        git 'https://github.com/ankit-nagarrogit/nx-workspace.git' // update this with your actual Git URL
      }
    }

    stage('Install Dependencies') {
      steps {
        bat 'npm install'
      }
    }

    stage('Build first-app') {
      steps {
        bat 'npx nx build first-app'
      }
    }

    stage('Deploy first-app') {
      steps {
        bat '''
          mkdir C:\\var\\www\\first-app
          xcopy /E /I /H /Y apps\\first-app\\dist\\* C:\\var\\www\\first-app\\
          start /B npx serve -s "C:/var/www/first-app" -l 3001
        '''
      }
    }
  }
}
